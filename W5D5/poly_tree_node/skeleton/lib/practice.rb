def merge_sort(array)
    return array if array.length < 2

    mid = array.length / 2
    left
end



def bfs(array, target)

    array.each do |ele|
        array.each do |ele|


end

def dfs(root, target)
    return if root == target
    root.chilren.each do |child|
        dfs(child, target)
    end
end



array to track to initial pos?